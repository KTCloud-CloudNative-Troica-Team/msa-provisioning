- name: Install Python and Kubernetes library on master nodes
  hosts: main-master
  become: true
  tasks:
    - name: Install python3-pip
      yum:
        name: python3-pip
        state: present

    - name: Install kubernetes Python library
      pip:
        name:
          - kubernetes
          - PyYAML
        executable: pip3
