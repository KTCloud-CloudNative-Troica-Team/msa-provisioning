- name: Fetch join info for Kubernetes worker nodes
  hosts: main-master
  become: true
  tasks:
    - name: Get join command
      ansible.builtin.shell: kubeadm token create --print-join-command
      register: join_command_raw

    - name: Save join command
      ansible.builtin.set_fact:
        join_command: "{{ join_command_raw.stdout }}"

- name: Join Kubernetes cluster as worker nodes
  hosts: ap-northeast-2a-workers,ap-northeast-2b-workers
  become: true
  tasks:
    - name: Run join command
      ansible.builtin.shell: "{{ hostvars['main-master']['join_command'] }}
        --cri-socket unix:///var/run/containerd/containerd.sock"
      register: join_result
      failed_when:
        - join_result.rc != 0
        - "'already exists' not in join_result.stderr"
